{
  "name": "wea-app-proxy",
  "version": "2.0.0",
  "description": "A lightweight proxy server which allows consumers to register callback mechanisms, such that the consumer can be notified before and after dialog is called",
  "main": "index.js",
  "keywords": [
    "swagger"
  ],
  "license": "MIT",
   "scripts": {
    "test": "node_modules/.bin/mocha --timeout 15000 --reporter spec"
  },
  "dependencies": {
    "connect": "^3.2.0",
    "js-yaml": "^3.3.0",
    "swagger-tools": "0.9.*",
    "watson-developer-cloud": "https://github.com/doconnor78/node-sdk",
    "vcap_services": "^0.1.7",
	  "mocha": "2.3.1",
	  "chai": "3.2.0", 
	  "chai-http":"1.0.0"
  }
}
